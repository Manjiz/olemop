{
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "publish": "lerna run build && lerna publish",
    "release": "lerna publish --force-publish=* --exact --skip-temp-tag"
  },
	"devDependencies": {
		"lerna": "^2.11.0"
	}
}
